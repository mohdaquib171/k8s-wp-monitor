apiVersion: v1
kind: PersistentVolume
metadata:
  name: wp-pv
spec:
  capacity:
    storage: 10Gi
  accessModes:
       - ReadWriteMany
  persistentVolumeReclaimPolicy: Recycle
  awsElasticBlockStore:
    volumeID: ebs-vol-id
    fsType: ext4
